name: Publish Guide

on:
  workflow_call:
    inputs:
      docs_dir:
        required: false
        type: string
        default: './docs'
      version:
        required: false
        type: string
        default: '1.0'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout this repo
        uses: actions/checkout@v5

      # TODO: integrate into gitbook
